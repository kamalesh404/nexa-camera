# GitHub Actions

The build workflow uses Java 17, Android SDK command-line tools, SDK platform 35, Build Tools 35.0.0, NDK 27.2.12479018, CMake 3.30.5, and Gradle 8.10.2. It runs unit tests and uploads `app-debug.apk` from the workflow artifacts.
