# Camera2 inspection

The lab enumerates `CameraManager.cameraIdList`, reads `CameraCharacteristics`, and inspects `StreamConfigurationMap` output sizes. Missing keys are reported as `not exposed`; no vendor capability is inferred. The exported JSON is the source of truth for later phases.
